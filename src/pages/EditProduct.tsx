import { useEffect, useRef, useState } from 'react';
import { ArrowLeft, ImagePlus, LoaderCircle } from 'lucide-react';
import { useNavigate, useParams } from 'react-router-dom';
import Sidebar from '../components/Sidebar';
import { loadSellerProduct, updateSellerProduct } from '../api/seller';
import type { SellerProductInput } from '../types/seller';

const empty: SellerProductInput = { name: '', game: '', type: 'Single', cost: 0, price: 0, stock: 0, imageUrl: null, productSet: '', language: 'English', description: '' };

const EditProduct = () => {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const fileRef = useRef<HTMLInputElement>(null);
  const [form, setForm] = useState<SellerProductInput>(empty);
  const [isLoading, setIsLoading] = useState(true);
  const [isSaving, setIsSaving] = useState(false);
  const [error, setError] = useState<string | null>(null);
  useEffect(() => {
    const productId = Number(id);
    if (!Number.isInteger(productId)) { setError('Invalid product id.'); setIsLoading(false); return; }
    loadSellerProduct(productId).then((product) => setForm({ name: product.name, game: product.game, type: product.type, cost: product.cost, price: product.price, stock: product.stock, imageUrl: product.imageUrl, productSet: product.productSet ?? '', language: product.language ?? '', description: product.description ?? '' })).catch((requestError) => setError(requestError instanceof Error ? requestError.message : 'Could not load product.')).finally(() => setIsLoading(false));
  }, [id]);
  const update = (key: keyof SellerProductInput, value: string | number | null) => setForm((current) => ({ ...current, [key]: value }));
  const loadImage = (file?: File) => { if (!file) return; const reader = new FileReader(); reader.onload = () => update('imageUrl', String(reader.result ?? '')); reader.readAsDataURL(file); };
  const submit = async (event: React.FormEvent) => { event.preventDefault(); setIsSaving(true); setError(null); try { await updateSellerProduct(Number(id), form); navigate('/seller'); } catch (requestError) { setError(requestError instanceof Error ? requestError.message : 'Could not update product.'); } finally { setIsSaving(false); } };
  const input = 'mt-1 w-full rounded-lg border border-slate-200 bg-white px-3 py-2.5 text-sm outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/10';
  if (isLoading) return <div className="flex min-h-screen items-center justify-center text-slate-500">Loading product…</div>;
  return <div className="flex min-h-screen bg-slate-50 text-slate-800"><Sidebar currentTab="stocks" /><main className="min-w-0 flex-1 p-8"><div className="mx-auto max-w-4xl"><button type="button" onClick={() => navigate('/seller')} className="mb-6 inline-flex items-center gap-2 text-sm font-bold text-slate-600 hover:text-blue-600"><ArrowLeft className="h-4 w-4" />Back to Stocks</button><div className="rounded-2xl border border-slate-200 bg-white p-8 shadow-sm"><p className="text-xs font-bold uppercase tracking-[0.2em] text-blue-600">Marketplace listing</p><h1 className="mt-1 text-3xl font-bold text-slate-900">Edit product</h1><p className="mt-2 text-sm text-slate-500">Saving changes sends the listing back to Pending for Admin review.</p>{error && <div className="mt-5 rounded-lg border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">{error}</div>}<form onSubmit={submit} className="mt-8 space-y-6"><div className="grid gap-5 md:grid-cols-2"><label className="text-sm font-semibold">Product name<input required value={form.name} onChange={(event) => update('name', event.target.value)} className={input} /></label><label className="text-sm font-semibold">Card game<input required value={form.game} onChange={(event) => update('game', event.target.value)} className={input} /></label><label className="text-sm font-semibold">Product type<select value={form.type} onChange={(event) => update('type', event.target.value)} className={input}><option>Single</option><option>Booster</option><option>Booster Box</option><option>Accessories</option></select></label><label className="text-sm font-semibold">Language<input value={form.language ?? ''} onChange={(event) => update('language', event.target.value)} className={input} /></label><label className="text-sm font-semibold">Set<input value={form.productSet ?? ''} onChange={(event) => update('productSet', event.target.value)} className={input} /></label><label className="text-sm font-semibold">Stock<input required min="0" type="number" value={form.stock} onChange={(event) => update('stock', Number(event.target.value))} className={input} /></label><label className="text-sm font-semibold">Cost price (฿)<input required min="0" step="0.01" type="number" value={form.cost} onChange={(event) => update('cost', Number(event.target.value))} className={input} /></label><label className="text-sm font-semibold">Selling price (฿)<input required min="0" step="0.01" type="number" value={form.price} onChange={(event) => update('price', Number(event.target.value))} className={input} /></label></div><label className="block text-sm font-semibold">Description<textarea rows={5} value={form.description ?? ''} onChange={(event) => update('description', event.target.value)} className={input} /></label><div><p className="text-sm font-semibold">Product image</p><div className="mt-2 flex items-center gap-4"><button type="button" onClick={() => fileRef.current?.click()} className="inline-flex items-center gap-2 rounded-lg border border-slate-300 px-4 py-2.5 text-sm font-bold hover:bg-slate-50"><ImagePlus className="h-4 w-4" />Choose image</button><input ref={fileRef} type="file" accept="image/*" onChange={(event) => loadImage(event.target.files?.[0])} className="hidden" />{form.imageUrl && <img src={form.imageUrl} alt="Preview" className="h-20 w-16 rounded object-cover ring-1 ring-slate-200" />}</div></div><div className="flex justify-end gap-3 border-t border-slate-100 pt-6"><button type="button" onClick={() => navigate('/seller')} className="rounded-lg border border-slate-300 px-5 py-2.5 text-sm font-bold">Cancel</button><button disabled={isSaving} type="submit" className="inline-flex items-center gap-2 rounded-lg bg-blue-600 px-5 py-2.5 text-sm font-bold text-white hover:bg-blue-700 disabled:opacity-60">{isSaving && <LoaderCircle className="h-4 w-4 animate-spin" />}Save and resubmit</button></div></form></div></div></main></div>;
};

export default EditProduct;
